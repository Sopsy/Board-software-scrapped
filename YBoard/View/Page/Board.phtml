<header class="page-header">
    <h1><?= htmlspecialchars($board->name) ?><?= ($pageNum>1?' sivu ' . $pageNum:'') ?></h1>
    <h2><?= htmlspecialchars($board->description) ?></h2>
</header>

<?php include($viewFilesPath . '/Snippets/PostForm.phtml') ?>

<?php foreach($threads as $thread) : ?>
<div class="content-box thread">
    <h3><a href="/<?= $board->url ?>/<?= $thread->id ?>"><?= htmlspecialchars($thread->subject) ?></a></h3>
    <div class="thread-content">
        <div class="op-post">
            <?php $post = $thread; include($viewFilesPath . '/Snippets/Post.phtml') ?>
        </div>
    <?php if (!empty($thread->replies)) : ?>
        <div class="thread-meta">
            1920 vastausta, 609 tiedostoa
        </div>
        <div class="replies">
            <button class="button gray">Avaa lanka</button>
            <button class="button gray">Näytä lisää vastauksia</button>
            <?php foreach($thread->replies as $reply) : ?>
                <div class="reply">
                    <?php $post = $reply; include($viewFilesPath . '/Snippets/Post.phtml') ?>
                </div>
            <?php endforeach ?>
        </div>
    <?php endif ?>
        <div class="thread-buttons">
            <button class="button gray">Vastaa</button>
            <button class="button gray">Päivitä</button>
        </div>
    </div>
</div>

<?php endforeach ?>
